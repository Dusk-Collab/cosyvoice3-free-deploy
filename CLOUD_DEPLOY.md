# CosyVoice 云端 GPU 部署指南（临时方案，等魔搭 xGPU 通过即换）

目标：找一个**免费、时长多、不用时不计时**的云端 GPU，先把 CosyVoice 跑成公网网址；
等魔搭 xGPU 审核通过后，直接用同一套脚本切到魔搭即可。

---

## 一、推荐平台：腾讯云 Cloud Studio（最符合"时长多 + 不用不计时"）

| 项目 | 说明 |
|---|---|
| 免费额度 | **每月 10000 分钟 GPU ≈ 166 小时/月**（T4，无框架限制，能跑 PyTorch） |
| 不用时不计时 | ✅ **10 分钟无操作自动关机**，关机后停止计费/计时 |
| 实名 | 需实名认证（一次性，平台要求） |
| 公网网址 | 平台自带"访问"按钮，自动把 7860 端口暴露成公网链接 |
| 适合 | 个人偶尔用完全够，时长在所有免费云端里最大 |

> 为什么选它：你要求"时长多的 + 不用不计时"。飞桨 360h/月看着最大，但只支持 PaddlePaddle、跑不了 CosyVoice；腾讯云 166h 是**能跑 CosyVoice 的免费云端里时长最多的**，且空闲自动停机不计费。

### 部署步骤（你只需做一次）

1. 打开 https://cloudstudio.net/ ，用微信/QQ 登录并**完成实名认证**。
2. 新建工作空间 → 选 **GPU 模板**（带 PyTorch/CUDA 的镜像，如"AI 模板"）→ 等待启动。
3. 进入工作空间后，打开**终端**，执行：
   ```bash
   bash <(curl -fsSL https://raw.githubusercontent.com/Dusk-Collab/cosyvoice3-free-deploy/main/cloud_deploy.sh)
   ```
   （脚本会克隆代码、装依赖、自动下载模型、后台启动服务）
4. 脚本跑完后，点工作空间里的 **"访问 / 公开链接"**，选择端口 **7860**，即可拿到公网网址。
5. 用完直接关掉网页即可；**10 分钟无操作平台自动关机，不再消耗额度**。

### 模型缓存在哪、会不会重复下载？（重要）

**不会。** Cloud Studio 工作空间磁盘是**持久化**的（官方：休眠/关闭后仍保留存储，删除才释放）。

- 模型下载到 `~/cosyvoice-deploy/.modelscope_cache`（脚本已写死这个路径）。
- 你每天用完关机、第二天再开，**那 5GB 模型还在，直接复用，不重新下载**。
- 只有两种情况会清空、导致重下 5GB：
  1. 你手动点了「**重置工作空间 / 删除**」；
  2. **免费高性能版**闲置超过 **30 天**被系统自动回收（罕见）。
- 想彻底避开第 2 种：① **每 30 天内登录打开一次工作空间即可**——打开后闲置倒计时会重新算，等于变相永久保留（注意：30 天是腾讯云固定规则，用户无法改成 90 天，但靠"每月点开一次"就能一直保住）；或 ② 把 `.modelscope_cache` 备份到腾讯云 COS 对象存储做兜底（按需再做）。

---

## 二、备选：Modal（零实名、idle=0，但时长少些）

如果你不想实名，可选 **Modal**（GitHub 登录即可、无需信用卡）：

- 免费档 **$30/月 算力额度 ≈ 50 小时 T4**，且 **空闲 = 0 计费**（不用时真的 1 秒都不算）。
- 时长比腾讯云少，但对"偶尔生成几条"完全够，且是**最干净的"不用不计时"**。
- 部署需我把 `app.py` 包成 Modal 的 `@stub.function` 形式（可随时再做）。

---

## 三、切回魔搭 xGPU（审核通过后）

魔搭 xGPU 标注"永久免费"，是最终理想（不占你显卡、不限额）。
审核通过后，在魔搭 Studio 新建实例、克隆同一仓库、跑同一个 `cloud_deploy.sh` 即可，
模型行改成 `FunAudioLLM/Fun-CosyVoice3-0.5B-2512`，端口由魔搭环境变量接管。

---

## 四、登录保护（防白嫖额度）

在 `cloud_deploy.sh` 里取消这两行注释并改成你的账号密码：
```bash
export COSYVOICE_USER=boss
export COSYVOICE_PASS=123456
```
重启服务后即需登录才能用，陌生人无法消耗你的免费额度。
