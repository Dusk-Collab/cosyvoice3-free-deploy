# CosyVoice 语音合成 · 小白使用说明（永久保存版）

> ⚠️ 这份说明存在你的 GitHub 仓库里，**比任何聊天记录都长久**。
> 就算你换电脑、离职、聊天记录没了——只要登录 GitHub（账号 **Dusk-Collab**）打开下面这个网址，就能看到这份说明，照着做就行：
>
> 👉 **https://github.com/Dusk-Collab/cosyvoice3-free-deploy**
>
> （建议用浏览器收藏夹把上面这个网址收藏起来，以后直接点开）

---

## 一、你有两种用法

### 用法 A：在自己电脑上用（免费、无限、最快）
- 双击桌面上的 **`CosyVoice本地打开.bat`**
- 自动打开浏览器，就能用了
- ✅ 优点：不花一分钱、想生成多少生成多少、数据全在自己电脑
- ⚠️ 缺点：换别人电脑 / 手机用不了（除非开隧道）

### 用法 B：用云端网址（免费、随处能用、但有限额）
- 用「腾讯云 Cloud Studio」部署，拿到一个公网网址，手机 / 别的电脑都能打开
- ✅ 优点：不用自己的显卡、随处可用
- ⚠️ 缺点：每月有免费额度（约 166 小时），不用时不计时

---

## 二、云端那份如果没了（被回收 / 误删），怎么重新弄？

**不用慌，全部自动，你只要做 4 步：**

1. 打开 https://cloudstudio.net/ 登录（微信 / QQ 都行）
2. 新建工作空间 → 选 **GPU 模板**（带 PyTorch / CUDA 的）→ 等它启动
3. 打开终端（黑框），粘贴运行**这一行**（就一行，复制粘贴回车）：
   ```
   bash <(curl -fsSL https://raw.githubusercontent.com/Dusk-Collab/cosyvoice3-free-deploy/main/cloud_deploy.sh)
   ```
4. 等它自己跑完（会自动下载代码 + 装依赖 + 下载模型），然后点「访问 / 公开链接」拿到网址

> 💡 所谓"重新下载"你**不用手动找文件**——上面这一行命令把什么都自动下好了。
> 模型下完存在云端「持久盘」，平时关机再开不用重下；只要**每 30 天登录打开一次**就不会被系统回收。

---

## 三、想加密码（防止别人白嫖你的额度）

在 `cloud_deploy.sh` 里，把这两行最前面的 `#` 去掉，改成你自己的账号密码：

```
export COSYVOICE_USER=你的用户名
export COSYVOICE_PASS=你的密码
```

详细操作看仓库里的 `CLOUD_DEPLOY.md`。

---

## 四、魔搭 xGPU（最终理想：永久免费、不占你显卡、不限额）

等「魔搭 xGPU乐园」组织审核通过，就能换成"永久免费 + 不占自己显卡 + 不限额"的方案。
审核状态在魔搭 Studio（https://www.modelscope.cn/studios）里查看。

---

## 五、⚠️ 防忘关机（最重要，不然会烧额度 / 欠费）

> 之前那次欠费，就是工作空间一直“运行中”、连续跑了约 130 小时把月度额度烧光导致的。
> 原因：**免费 GPU 的“10 分钟自动关”是按心跳算的——你只要开着浏览器标签页，它就一直算“有人用”，永远不关。**
> 所以**不能靠“放着不动”让它自己关**，必须主动关。

**启动云端后，务必做这一步（最关键）：**
1. 进工作空间，跑完 `cloud_deploy.sh` 拿到网址；
2. **立刻点编辑界面顶部栏的「设置定时关机」，选 2~4 小时**（最长 24 小时）；
3. 到点系统**无条件硬关机**，哪怕你忘了关标签页也不烧钱。
4. 想立刻停：点工作空间列表「关机」按钮，或关掉标签页等 10 分钟。

**记住三句话：**
- 「运行中」= 一直在烧额度；「关机」= 不烧。
- 开着网页 ≠ 会自动关，反而一直计费。
- 最稳的保险 = 平台「定时关机」按钮，每次启动都设一下。

---

## 六、重要提醒

- 本机桌面那份（`CosyVoice本地打开.bat`）和云端那份是**两套独立的东西**，互不影响。
- 本机那份永远免费、无限量；云端那份消耗免费额度（每月约 166 小时，每月 1 号重置），**不用时必须关机才停计费**。
- 所有部署代码都在本仓库，随时能重新部署，不用担心丢。
